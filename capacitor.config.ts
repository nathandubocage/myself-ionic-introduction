import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'myself-ionic-introduction',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
